package org.example.funcs;

import org.example.model.Expression;

public class ArithmeticNormalizer {

    /*
    public sealed interface Result {}
    public record Success() implements Result {}
    public record Failure() implements Result {}

    private record Step(Expression.CanonicalTerm normalizedTerm, CurrentSpace currentSpace) {}
    private enum CurrentSpace {
        NUM, DENOM
    }

    public static Step normalizeArithmetic(Expression.CanonicalTerm canonicalTerm) {

    }

    private static Step normalizeBinaryTerm(Expression.CanonicalBinaryTerm canonicalBinaryTerm) {
        Step left = normalizeArithmetic(canonicalBinaryTerm.left());
        Step right = normalizeArithmetic(canonicalBinaryTerm.right());
        return switch (canonicalBinaryTerm.operator()) {
            case MUL -> {
                left.normalizedTerm()
            }
        }
    }

     */
}
